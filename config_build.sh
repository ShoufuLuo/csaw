#!/bin/bash

# Build config for the build script, build.sh. Look there for more info.

APP_NAME=Pinocchio
CHROME_PROVIDERS="content locale skin"
CLEAN_UP=0
ROOT_FILES="README"
ROOT_DIRS="components default"
BEFORE_BUILD= #"sh xpcom_build.sh"
AFTER_BUILD=
